# Flat node_modules so native modules (sharp/libvips) resolve their sibling
# .so files at runtime on Vercel. pnpm's default symlinked layout breaks
# sharp's dlopen of libvips-cpp.so under Next's serverless file tracing.
node-linker=hoisted
